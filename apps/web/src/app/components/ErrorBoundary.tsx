/**
 * ErrorBoundary Component
 * 
 * Thin re-export wrapper for backward compatibility.
 * Canonical implementation is in components/shared/ErrorBoundary
 * 
 * @deprecated Import from '../components/shared/ErrorBoundary' instead
 */

export { ErrorBoundary } from './shared/ErrorBoundary';
